<% if current_user.liked?(micropost) %>
  <%= link_to 'disLike', micropost_favorites_path(micropost), method: :delete, class: 'btn btn-danger btn-xs' %>
<% else %>
  <%= link_to 'Like', micropost_favorites_path(micropost), method: :post, class: 'btn btn-primary btn-xs' %>
<% end %>